server:
  addr: 127.0.0.1:8000

database:
  host: localhost
  port: 5432
  
  root:
    username: alex
    password: ""
    database: postgres

  application:
    username: uptimeblue
    password: test
    database: uptime

routing:
  sns_topic: ""

metrics:
  endpoint: "http://localhost:9090/api/v1/otlp/v1/metrics"
  interval_seconds: 15
