# `dns-server`

A DNS server implementation that acts as a recursive resolver backed by
`all.dns.mullvad.net` (which blocks various types of content such as adverts,
trackers and social media) as well as allowing custom domains to be blocked.
